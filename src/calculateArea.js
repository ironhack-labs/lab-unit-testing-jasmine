function calculateArea(num1, num2) {
    if (calculateArea.length === 2) {
        return num1 * num2
    }
    else if (calculateArea.length === 0) {
        return undefined
    }


}

const numbersArray = [7, 2]