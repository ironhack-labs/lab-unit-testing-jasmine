function printFullName(objName) {
    if(typeof objName !== "object"){
        return undefined
    }
    if (typeof obj.firstName !== "string" || obj.lastName !== "string"){
    return undefined
}

return obj.firstName + " " + obj.lastName
}