function printFullName(/*{firstName, lastName}*/) {
    
   /* if (printFullName() !== "object") {
        return undefined; 
    }

    if (firstName == undefined || lastName == undefined) {
        return undefined; 
    }
    
    
    return `${firstName} ${lastName}` */
} 