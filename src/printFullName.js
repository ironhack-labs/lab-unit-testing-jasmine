function printFullName(object) {

    if (printFullName.length === 2) {
        return `${objectArray.firstName}" "${objectArray.lastName}`
    }
    else if (object !== typeof Object) {
        return undefined
    }
    else {
        return undefined
    }


}

const objectArray = {

    firstName: "Alejandro",
    lastName: "Magro"
}
