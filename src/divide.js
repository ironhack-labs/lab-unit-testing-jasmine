// function divide(a, b) {
//     if (typeof a !== 'number' || typeof b !== 'number') {
//         return undefined; // Return undefined if any argument is not a number
//     }

//     return result; // Perform division and return the result
// }
function divide(a, b) {
    if (typeof a !== "number" || typeof b !== "number") {
        return undefined
    }
    return a/b;
}